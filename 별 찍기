/*
	재귀 / 별찍기 / 3의 제곱수를 받아서 내가 원하는대로 별을 찍어보자
	2022년 5월 24일 / 홍주안 화이팅
*/

#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

void star(int i, int j, int n);

int main(void)
{
	int n;
	scanf("%d", &n); //입력을 받음

	for (int i = 0; i < n; i++)
	{
		for (int j = 0; j < n; j++)
		{
			star(i, j, n);
		}
		printf("\n");
	}
}

void star(int i, int j, int n)
{
	if ((i / n) % 3 == 1 && (j / n) % 3 == 1)// i, j가 n의 조화로 가운데 빈 공간이 만들어짐
		printf(" ");
	else
	{
		if (n / 3 == 0)// 3의 배수인 경우 *을 출력해주는 조건
		{
			printf("*");
		}
		else
			star(i, j, n / 3); // i와 j는 그대로 가기에 사실상 n만 3으로 나눠주는 조건이고 이것은 곧 *을 출력하는 조건
	}						   
		// 이 문제에서의 재귀함수는 표를 3등분으로 나눠서 다시 재귀로 돌아가서 계속 불러주는 조건이 아닌
		// 한줄 한줄을 출력하는데에서 조건을 얻기위해 재귀함수를 사용함
}



/*
	9일 경우

	빈 공간 ***
			***
			***
	n 은 순서대로 333
				333
				333
	으로 빈칸 출력하는 함수의 조건에 충족한다  

	i는 순서대로 333
	             444  전부다 3으로 나누면 몫이 1
				 555

	j는 순서대로 345
				 345 전부다 3으로 나누면 몫이 1
				 345

	그래서 빈 공간이 출력이 된다 
	3의 다른 제곱또한 같은 원리이다
				 */
